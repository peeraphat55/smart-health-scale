name: project
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  fl_chart: ^1.1.1
  google_fonts: ^4.0.3
  percent_indicator: ^4.2.2
  provider: ^6.1.1
  curved_navigation_bar: ^1.0.6
  flutter_blue_plus: ^2.2.1
  permission_handler: ^12.0.1
  tflite_flutter: ^0.12.1
  flutter_bluetooth_serial_ble: ^0.5.0
  
  # 🟢 ปลดล็อกเลขเวอร์ชัน Firebase ทั้งหมด เพื่อให้ระบบดึงตัวล่าสุดที่ทำงานเข้าขากัน 100% มาให้เอง
  firebase_core: 
  firebase_database: 
  cloud_firestore: 
  firebase_auth: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # 🟢 ย้ายโมเดล tflite ทั้ง 2 ตัวมารวมไว้ในหมวด flutter ให้ถูกต้อง
  assets:
    - assets/health_model.tflite
    - assets/bmi_trend_model.tflite